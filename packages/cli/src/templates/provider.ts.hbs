import { createCacheDecorator } from '@mobilejazz/harmony-core';

// Caching via decorator
const Cached = createCacheDecorator();

export abstract class {{ pascalCase name }}Provider {

}

export class {{ pascalCase name }}DefaultProvider implements {{ pascalCase name }}Provider {

}
